# 이어가 현장 검증 플레이북

이 문서는 코드 테스트를 실제 여행 현장 증거로 바꾸기 위한 실행 계약이다.
템플릿의 빈 행은 증거가 아니며, 실제 참여자·실기기·실제 이동으로 측정된
원장만 출시 증거에 포함한다.

## 공통 원칙

1. 운영 배포 URL과 제출 예정 커밋 SHA를 모든 원장에 기록한다.
2. 성공 여부뿐 아니라 실패 코드, 잘못 추천된 후보, 네트워크 상태를 함께 기록한다.
3. 개인의 정확한 좌표·건강정보·자유서술 원문은 저장하지 않는다.
4. 참여자 ID는 연구용 난수 ID로 만들고 연락처와 분리한다.
5. 추천이 안전하지 않거나 근거가 불충분하면 즉시 중단하고 실패로 기록한다.
6. 목표치를 실측치처럼 발표하지 않는다.

## 등록과 독립 감사 승인

현장 수행자와 OPS 제출자는 증거를 승인할 수 없다. 제출 권한에는
`OPS_API_KEY`, 승인 권한에는 별도 담당자가 관리하는
`RELEASE_AUDITOR_API_KEY`를 사용한다. 두 키를 포함한 출시용 Secret 4개는
각각 32바이트 이상이고 모두 서로 달라야 한다.

1. 증거 원장을 `https://...#sha256=<64 hex>` 또는
   `r2://REGION_PACKS/field-evidence/...#sha256=<64 hex>`로 고정하고
   `POST /api/v1/ops/evidence`에 표본·권역·지표·측정시각·검토자와 함께
   등록한다. 단독 해시, 허용목록 밖 HTTPS, 리다이렉트, 사라진 원본은 승인할
   수 없다.
2. 응답의 evidence ID와 `validated_pending_independent_audit` 상태를
   기록한다. `validated`는 형식·180일 기간·표본·임계값 통과일 뿐이다.
3. 독립 감사자는 `GET /api/v1/auditor/evidence/{evidenceId}`로 artifact와
   메타데이터를 읽고 원본 무결성, 실제 수행 기록, 검토자 역할을 대조한다.
   서버도 승인 시 원본 바이트를 읽어 SHA-256을 다시 계산한다.
4. 통과하면 같은 경로에 `PATCH`로
   `{ "decision": "approved", "approvedBy": "감사자 식별자" }`를 보내고,
   실패하면 `decision: "rejected"`와 필수 `notes`에 사유를 기록한다.
5. 결정은 `pending`에서 한 번만 가능하다. 반려 또는 만료된 증거를
   수정·재승인하지 말고 새 evidence ID로 다시 제출한다.

출시 보고서는 형식 검증과 독립 `approved`가 모두 충족된 증거만
`verified`로 계산한다. 감사 키 부재·짧은 키·다른 역할 키 재사용은
즉시 출시 차단 사유다.

## K-TRIPBREAK 100

### 표본 구성

- 지역: 수도권 20, 광역시 25, 일반 시군 25, 도서·산간 15, 부산 파일럿 15
- 사건: 우천 25, 혼잡 25, 지연 25, 임시 휴무·운영변경 25
- 이용자: 일반 40, 유아차 20, 휠체어 20, 고령자 20
- 일정 조건: 다음 고정예약 있음 70, 없음 30
- 시간대: 오전 25, 점심 25, 오후 25, 저녁 25

### 통과 조건

- 실내·접근성·운영시간 등 필수 근거가 없으면 `verified` 금지
- 고정 예약 도착시간 위반 대안을 적용 가능으로 표시한 건 0
- 폐업·휴무·목적 불일치·접근성 불일치 치명적 오추천 0
- 추천 없음 결과도 원인과 사용자가 취할 다음 행동을 제시
- 각 실행의 request ID, rule version, source ledger, 응답시간을 보존

## 사람·현장·비교 증거의 필수 표본

다음 항목은 코드 테스트나 AI 페르소나로 대체할 수 없는 **제출 차단 게이트**다.
모집이 어렵다는 이유로 항목을 빼거나 표본을 낮추지 않는다.

- 독립 실사용자 20명 이상. 한국어·영어를 포함한 3개 이상 로케일과 초행자,
  이동약자 또는 동행자 표본을 포함한다.
- 실제 이동 12회 이상. 6개 권역 유형을 포함하며 부산 현장 이동을 최소
  5회 포함한다.
- 관광·지자체·접근성 분야의 독립 실무자 3명 이상. 제출자와 같은 사람이
  승인할 수 없고 치명적 미해결 의견이 0건이어야 한다.
- 실제 외부 파트너 1곳 이상에서 iframe·메모리 전용 bearer·복구 완료를
  Chrome·Safari·Firefox 모바일 브라우저 3종으로 총 4회 이상 실증한다.
- 동일 20개 상황에서 이어가·수작업 검색·범용 생성형 AI·일반 일정 재생성기를
  같은 입력과 시간 제한으로 비교한다. 각 방법의 실패도 삭제하지 않고 남긴다.
- 모든 참여자 원장은 연구용 난수 ID만 사용하고, 동의 원본은 결과 원장과
  분리한다. 공개 제출에는 가명 동의 원장만 포함하고, 그 원장을 별도
  `participant_consent_ledger` 증거로 등록해 원본 바이트·SHA-256·독립 승인을
  통과시킨다. 참여자 행의 `consent_reference`는
  `approved-consent-ledger:sha256:<원장 SHA-256>`으로 그 승인 원장과 정확히
  연결한다. 단독 해시 문자열은 허용하지 않는다.

실사용자 과업 완료율은 90% 이상, 추천 근거 이해도와 신뢰도는 각각 평균
4.2/5 이상이어야 한다. 고정 일정 훼손, 휴무지 적용 가능 추천, 실제 예약
미준수 등 치명적 안전 사건은 표본 수와 관계없이 단 한 건도 허용하지 않는다.

## 성능·접근성

- 실기기: 저사양 Android, 최신 Android, iPhone, 태블릿, 데스크톱
- 네트워크: Wi-Fi, LTE/5G, Slow 4G, 순간 단절
- 뷰포트: 360, 390, 768, 1280px
- 접근성: 키보드 전용, 화면읽기, 200% 확대, 모션 감소, 고대비
- 보고값: p50, p95, 오류율, 재시도 성공률
- 실측 40회 이상, 그중 모바일 실기기 20회 이상과 LTE/Slow 4G 16회 이상
- 복구 전체 응답 p50 4초 이하, p95 8초 이하
- Core Web Vitals p75: LCP 2.5초 이하, INP 200ms 이하, CLS 0.1 이하
- axe WCAG A·AA serious/critical 0건, Lighthouse 접근성 100점

## 파트너 iframe 실증

- 계약된 외부 HTTPS origin을 최소 1곳 사용하고 와일드카드는 금지한다.
- iOS Safari, Android Chrome, 모바일 Firefox 3종을 모두 포함해 총 4회 이상
  실제 파트너 페이지에서 수행한다.
- iframe 로드, 위치 권한 위임, 메모리 전용 10분 embed bearer bootstrap,
  복구 API 완료율은 각각
  100%여야 하며 치명적 실패는 0건이어야 한다.
- 위젯은 타사 쿠키나 CHIPS에 의존하지 않는다. bootstrap bearer는
  `recover:open-window` 전용이고 브라우저 저장소에 기록하지 않으며 만료·변조·
  일정 복구·분석 동의 요청은 서버가 거절한다. Safari·Firefox·Chrome에서
  타사 쿠키 차단 상태로도 이 계약을 각각 실증한다.

## 출시 게이트

다음이 모두 충족되기 전에는 `ready`로 표시하지 않는다.

- 운영 의존성 High 취약점 0
- 관리형 경로·날씨 제공자
- K-TRIPBREAK 100 완료 및 치명적 오추천 0
- 고정 예약 원본 시각 보존 100%, 휴무·운영 미확인 적용 가능 추천 0건
- 후보가 있는 고정 예약 시나리오 최소 50건의 원본 시각 보존 100%
- 후보가 있는 비고정 시나리오 최소 15건의 역방향 복귀 경로 확인 100%
- 적용 대상이 아닌 행은 `not_applicable`로 기록하고 성공 모수에서 제외
- 독립 실사용자 20명, 현장 이동 12회, 실무자 3인, 동일 조건 비교 20상황
- 외부 파트너 1곳, Chrome·Safari·Firefox 모바일 3종, iframe 전체 복구 4회
- 모든 현장 증거가 제출자와 분리된 독립 감사자에게 `approved`
- 실기기 성능 p50·p95 원장 확보
- 배포 브라우저 E2E·접근성·대비·PWA 검사 통과

## 기계 검증

`evidence/templates/`의 CSV는 빈 양식일 뿐 증거가 아니다. 실제 원본을 수집한
후 각 CSV의 SHA-256과 제출 SHA·배포 URL을 제출 매니페스트에 고정하고 다음
명령을 실행한다.

```powershell
node scripts/submission-gate.mjs --manifest evidence/submission-manifest.json
```

게이트는 현재 Git HEAD, `origin/main`, 배포본의 `/api/v1/release/version`,
CSV 해시와 행별 commit/deployment 값을 서로 대조한다. 모든 제출 CSV의
SHA-256은 배포본 `/api/v1/release/evidence`가 공개하는 독립 승인·원본 검증
digest와 일치해야 한다. 행별 `artifact_reference`는 실제 바이트를 5MiB
한도와 redirect 차단 조건으로 다시 받아 fragment SHA-256을 계산하거나,
`approved-artifact:sha256:<digest>`로 배포본의 승인 digest와 정확히 연결해야
한다. 404, 임의 URL, `r2://` 모양, 단독 `sha256:` 문자열만으로는 통과하지
못한다. 하나라도 다르면 출시하지 않는다. CI에서는 `--structure-only`로
양식과 필수 열이 사라지지 않는지만 검사하며, 이를 현장 증거 통과로 해석하지
않는다.
