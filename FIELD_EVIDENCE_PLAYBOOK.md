# 이어가 현장 검증 플레이북

이 문서는 코드 테스트를 실제 여행 현장 증거로 바꾸기 위한 실행 계약이다.
템플릿의 빈 행은 증거가 아니며, 실제 참여자·실기기·실제 이동으로 측정된
원장만 출시 증거에 포함한다.

## 공통 원칙

1. 운영 배포 URL과 제출 예정 커밋 SHA를 모든 원장에 기록한다.
2. 성공 여부뿐 아니라 실패 코드, 잘못 추천된 후보, 네트워크 상태를 함께 기록한다.
3. 개인의 정확한 좌표·건강정보·자유서술 원문은 저장하지 않는다.
4. 참여자 ID는 연구용 난수 ID로 만들고 연락처와 분리한다.
5. 추천이 안전하지 않거나 근거가 불충분하면 즉시 중단하고 실패로 기록한다.
6. 목표치를 실측치처럼 발표하지 않는다.

## 등록과 독립 감사 승인

현장 수행자와 OPS 제출자는 증거를 승인할 수 없다. 제출 권한에는
`OPS_API_KEY`, 승인 권한에는 별도 담당자가 관리하는
`RELEASE_AUDITOR_API_KEY`를 사용한다. 두 키를 포함한 출시용 Secret 4개는
각각 32바이트 이상이고 모두 서로 달라야 한다.

1. 증거 원장을 `https://`, `r2://` 또는 `sha256:<64 hex>` artifact로
   고정하고 `POST /api/v1/ops/evidence`에 표본·권역·지표·측정시각·검토자와
   함께 등록한다.
2. 응답의 evidence ID와 `validated_pending_independent_audit` 상태를
   기록한다. `validated`는 형식·180일 기간·표본·임계값 통과일 뿐이다.
3. 독립 감사자는 `GET /api/v1/auditor/evidence/{evidenceId}`로 artifact와
   메타데이터를 읽고 원본 무결성, 실제 수행 기록, 검토자 역할을 대조한다.
4. 통과하면 같은 경로에 `PATCH`로
   `{ "decision": "approved", "approvedBy": "감사자 식별자" }`를 보내고,
   실패하면 `decision: "rejected"`와 필수 `notes`에 사유를 기록한다.
5. 결정은 `pending`에서 한 번만 가능하다. 반려 또는 만료된 증거를
   수정·재승인하지 말고 새 evidence ID로 다시 제출한다.

출시 보고서는 형식 검증과 독립 `approved`가 모두 충족된 증거만
`verified`로 계산한다. 감사 키 부재·짧은 키·다른 역할 키 재사용은
즉시 출시 차단 사유다.

## K-TRIPBREAK 100

### 표본 구성

- 지역: 수도권 20, 광역시 25, 일반 시군 25, 도서·산간 15, 부산 파일럿 15
- 사건: 우천 25, 혼잡 25, 지연 25, 임시 휴무·운영변경 25
- 이용자: 일반 40, 유아차 20, 휠체어 20, 고령자 20
- 일정 조건: 다음 고정예약 있음 70, 없음 30
- 시간대: 오전 25, 점심 25, 오후 25, 저녁 25

### 통과 조건

- 실내·접근성·운영시간 등 필수 근거가 없으면 `verified` 금지
- 고정 예약 도착시간 위반 대안을 적용 가능으로 표시한 건 0
- 폐업·휴무·목적 불일치·접근성 불일치 치명적 오추천 0
- 추천 없음 결과도 원인과 사용자가 취할 다음 행동을 제시
- 각 실행의 request ID, rule version, source ledger, 응답시간을 보존

## 초행 사용자 20명

참여자에게 기능 설명을 하지 않고 다음 과업을 순서대로 제시한다.

1. 위치 권한을 거부하고 현재 장소를 직접 검색한다.
2. 우천 사건과 다음 예약을 입력한다.
3. 대안의 미확인 조건과 제외 이유를 설명한다.
4. 하나의 대안을 적용하고 길찾기로 이동한다.
5. 도착을 확인하고 원래 일정으로 복귀한다.
6. 자신의 익명 세션 데이터를 삭제한다.

주 지표는 무도움 과업완료율, 첫 유효 대안까지 걸린 시간, 치명적 오류,
신뢰도 5점 척도다. 진행자의 설명이나 화면 조작이 들어가면 `assisted`로
구분하며 무도움 성공에 포함하지 않는다.

## 관광·지자체 실무자 3인

최소한 관광안내, 지자체 관광정책, 관광사업 운영 역할을 각각 한 명 포함한다.
검토자는 다음 항목을 서명한다.

- 여행자에게 제시된 다음 행동이 현장에서 수행 가능한가
- 장소·운영시간·접근성 근거의 책임주체가 명확한가
- 공개 미션이 담당조직·기한·성공조건·증거요건을 갖추었는가
- 내부 담당자명과 비공개 증빙 URL이 공개 응답에서 제거되었는가
- 같은 시나리오 재검증으로 개선 전후를 비교할 수 있는가

## 6개 권역 현장 감사

수도권, 부산, 다른 광역시, 일반 도시, 산간, 도서 권역을 각각 한 곳 이상
포함한다. 각 권역에서 장소검색, 경로, 운영정보, 접근성, 도착 계약을 실제
이동으로 확인한다. 직선거리 추정만 사용된 경우 경로 검증 성공으로 세지 않는다.

## 성능·접근성

- 실기기: 저사양 Android, 최신 Android, iPhone, 태블릿, 데스크톱
- 네트워크: Wi-Fi, LTE/5G, Slow 4G, 순간 단절
- 뷰포트: 360, 390, 768, 1280px
- 접근성: 키보드 전용, 화면읽기, 200% 확대, 모션 감소, 고대비
- 보고값: p50, p95, 오류율, 재시도 성공률

## 출시 게이트

다음이 모두 충족되기 전에는 `ready`로 표시하지 않는다.

- 운영 의존성 High 취약점 0
- 관리형 경로·날씨 제공자
- K-TRIPBREAK 100 완료 및 치명적 오추천 0
- 초행 사용자 20명 완료
- 실무자 3인 검토 완료
- 6개 권역 현장 감사 완료
- 모든 현장 증거가 제출자와 분리된 독립 감사자에게 `approved`
- 실기기 성능 p50·p95 원장 확보
- 배포 브라우저 E2E·접근성·대비·PWA 검사 통과
